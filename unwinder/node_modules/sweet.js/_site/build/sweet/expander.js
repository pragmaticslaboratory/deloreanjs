import {List} from "immutable";
import TermExpander from "./term-expander.js";
import TokenExpander from "./token-expander";
import {Scope, freshScope} from "./scope";
import  * as _ from "ramda";
export default class Expander {
  constructor(context_302) {
    this.context = context_302;
  }
  expand(stxl_303) {
    let tokenExpander_304 = new TokenExpander(this.context);
    let termExpander_305 = new TermExpander(this.context);
    return _.pipe(_.bind(tokenExpander_304.expand, tokenExpander_304), _.map(t_306 => termExpander_305.expand(t_306)))(stxl_303);
  }
}
